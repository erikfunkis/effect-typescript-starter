{
  "name": "@template/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo \"No shared lint targets yet\"",
    "check": "npm run lint && echo \"TODO: add shared typecheck\"",
    "test": "echo \"TODO: add shared tests\"",
    "build": "echo \"TODO: add shared build\""
  }
}
